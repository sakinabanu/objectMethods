// Object.entries : it converts into arrays.
let r = {
    fullname: "Sakina",
    age: 22,
    location: "Hyderabad",
};
// always converts in nested arrays only.
let result = Object.entries(r);
console.log(result);

// null datatype is object by mistake.